export const SET_NAME = (name) => {
  return {
    type: 'SET_NAME',
    name: name,
  };
};

export const SET_USERNAME = (username) => {
  return {
    type: 'SET_USERNAME',
    username: username,
  };
};

export const SET_EMAIL = (email) => {
  return {
    type: 'SET_EMAIL',
    email: email,
  };
};

export const SET_PROFILE_PIC = (profilePic) => {
  return {
    type: 'SET_PROFILE_PIC',
    profilePic: profilePic,
  };
};
